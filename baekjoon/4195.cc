#include <iostream>
#include <map>
#include <string>

using namespace std;

map<string, int> person;
int parent[100001];
int F, T;

int main() {
    ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);
    cin >> T;
    for(; T>0; T--) {
        int idx = 0;
        cin >> F;
        for(; F>0; F--) {
            string a, b;
            cin >> a >> b;
            
        }
    }
}