#include <bits/stdc++.h>

using namespace std;
int arr[1000001];
int count[1000001];
int tree[4000004];

int main () {
    ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);
    int N;
    cin >> N;
    for(int i=0; i<N; i++) {
        int t;
        cin >> t;
    }
}