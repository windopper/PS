#include <iostream>
#include <map>
#include <vector>

using namespace std;
int N, M;
map<vector<int>> arr;

int main() {
    ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);
    cin >> N >> M;
    for(int i=0; i<M; i++) {
        int a, b;
        cin >> a >> b;
    }
}