#include <bits/stdc++.h>

using namespace std;
int N, Q;

int main() {
    ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);
    cin >> N >> Q;
    for(int i=0; i<N-1; i++) {

    }
    while(Q--) {
        int b, c, d;
        cin >> b >> c >> d;
        if(d == 0) {
            
        }
        else {

        }
    }
}