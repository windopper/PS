#include <bits/stdc++.h>

using namespace std;
vector<int> arr;

void solve(int b) {

}


int main() {
    ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);
    int t;
    while(cin >> t) {
        arr.push_back(t);
    }
}